# Example env file (safe to commit)
# Copy to .env.local and fill real values (do NOT commit .env.local)
DATABASE_URL=postgresql://<user>:<password>@<host>:<port>/<db>?schema=public
SSH_USER=<ssh_user>
SSH_PASSWORD=<ssh_password>
