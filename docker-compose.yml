version: '3.8'

services:
  # PostgreSQL con PostGIS
  postgres:
    image: postgis/postgis:15-3.3-alpine
    container_name: monitoreo-db
    restart: unless-stopped
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_USER: ${DATABASE_USER:-transito}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-transito}
      POSTGRES_DB: ${DATABASE_NAME:-protransito}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - monitoreo-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USER:-transito} -d ${DATABASE_NAME:-protransito}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Backend NestJS
  backend:
    build:
      context: .
      dockerfile: ./docker/backend/Dockerfile
      target: development
    container_name: monitoreo-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      DATABASE_URL: postgresql://${DATABASE_USER:-transito}:${DATABASE_PASSWORD:-transito}@postgres:5432/${DATABASE_NAME:-protransito}?schema=public
      JWT_SECRET: ${JWT_SECRET:-mi-secreto-super-seguro-desarrollo-minimo-32-caracteres}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-mi-refresh-secreto-super-seguro-desarrollo-minimo-32}
      PORT: 3000
      HOST: 0.0.0.0
    ports:
      - "3000:3000"
    volumes:
      - ./apps/backend:/app/apps/backend
      - ./packages:/app/packages
      - backend_node_modules:/app/node_modules
      - backend_app_node_modules:/app/apps/backend/node_modules
    networks:
      - monitoreo-network
    command: npm run backend:dev

  # Frontend React
  frontend:
    build:
      context: .
      dockerfile: ./docker/frontend/Dockerfile
      target: development
    container_name: monitoreo-frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      VITE_API_URL: ${VITE_API_URL:-/api}
    ports:
      - "5173:5173"
    volumes:
      - ./apps/frontend:/app/apps/frontend
      - ./packages:/app/packages
      - frontend_node_modules:/app/node_modules
      - frontend_app_node_modules:/app/apps/frontend/node_modules
    networks:
      - monitoreo-network
    command: npm run frontend:dev

  # Nginx (Producción)
  nginx:
    image: nginx:alpine
    container_name: monitoreo-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./docker/nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - backend
      - frontend
    networks:
      - monitoreo-network
    profiles:
      - production

volumes:
  postgres_data:
    name: control-center_postgres_data
    external: true
  backend_node_modules:
  backend_app_node_modules:
  frontend_node_modules:
  frontend_app_node_modules:

networks:
  monitoreo-network:
    driver: bridge
